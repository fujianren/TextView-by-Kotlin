<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.cbb.kotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- <receiver android:name=".LonginReceiver" android:exported="false"> -->
        <!-- <intent-filter> -->
        <!-- <action android:name="com.cbb.kotlin"/> -->
        <!-- </intent-filter> -->
        <!-- </receiver> -->

        <activity android:name=".basic.TestActivity">
        </activity>
        <activity android:name=".loading_view.LoadingViewDemoActivity">
        </activity>
        <activity android:name=".LightTextActivity">
        </activity>
    </application>

</manifest>